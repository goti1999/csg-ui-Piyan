id: '17612221965130.6885042492935419'
kind: store_item
name: allColumns
nameConfigured: true
steps: []
mode: manual
modeConfigured: false
storeItem:
  value: |-
    [
      {
        "prop": "Timestamp",
        "type": "datetime",
        "title": "Timestamp",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search Timestamp"
        },
        "viewSettings": {
          "mapper": "{{row.Timestamp}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Interface",
        "type": "string",
        "title": "Interface",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Interface"
        },
        "viewSettings": {
          "mapper": "{{row.Interface}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "direction",
        "type": "string",
        "title": "direction",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search direction"
        },
        "viewSettings": {
          "mapper": "{{row.direction}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "CustomerID",
        "type": "string",
        "title": "CustomerID",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search CustomerID"
        },
        "viewSettings": {
          "mapper": "{{row.CustomerID}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "MBL_ID",
        "type": "number",
        "title": "MBL_ID",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "number",
          "placeholder": "Search MBL_ID"
        },
        "viewSettings": {
          "mapper": "{{row.MBL_ID}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "number",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ContainerID",
        "type": "number",
        "title": "ContainerID",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "number",
          "placeholder": "Search ContainerID"
        },
        "viewSettings": {
          "mapper": "{{row.ContainerID}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "number",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "MBL_Number",
        "type": "string",
        "title": "MBL_Number",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search MBL_Number"
        },
        "viewSettings": {
          "mapper": "{{row.MBL_Number}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Container_Number",
        "type": "string",
        "title": "Container_Number",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Container_Number"
        },
        "viewSettings": {
          "mapper": "{{row.Container_Number}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Status",
        "type": "string",
        "title": "Status",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Status"
        },
        "viewSettings": {
          "mapper": "{{row.Status}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "TransmissionTimestamp",
        "type": "datetime",
        "title": "TransmissionTimestamp",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search TransmissionTimestamp"
        },
        "viewSettings": {
          "mapper": "{{row.TransmissionTimestamp}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Voyage",
        "type": "string",
        "title": "Voyage",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Voyage"
        },
        "viewSettings": {
          "mapper": "{{row.Voyage}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ContainerSize",
        "type": "string",
        "title": "ContainerSize",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search ContainerSize"
        },
        "viewSettings": {
          "mapper": "{{row.ContainerSize}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Broker",
        "type": "string",
        "title": "Broker",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Broker"
        },
        "viewSettings": {
          "mapper": "{{row.Broker}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Carrier",
        "type": "string",
        "title": "Carrier",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Carrier"
        },
        "viewSettings": {
          "mapper": "{{row.Carrier}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Vessel",
        "type": "string",
        "title": "Vessel",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Vessel"
        },
        "viewSettings": {
          "mapper": "{{row.Vessel}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "PortOfDischarge_Lima",
        "type": "string",
        "title": "PortOfDischarge_Lima",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search PortOfDischarge_Lima"
        },
        "viewSettings": {
          "mapper": "{{row.PortOfDischarge_Lima}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ETA_PortOfDischarge",
        "type": "datetime",
        "title": "ETA_PortOfDischarge",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ETA_PortOfDischarge"
        },
        "viewSettings": {
          "mapper": "{{row.ETA_PortOfDischarge}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "PackingMethod",
        "type": "string",
        "title": "PackingMethod",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search PackingMethod"
        },
        "viewSettings": {
          "mapper": "{{row.PackingMethod}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "TotalWeight",
        "type": "number",
        "title": "TotalWeight",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "number",
          "placeholder": "Search TotalWeight"
        },
        "viewSettings": {
          "mapper": "{{row.TotalWeight}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "number",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "AdvertFlag",
        "type": "boolean",
        "title": "AdvertFlag",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search AdvertFlag"
        },
        "viewSettings": {
          "mapper": "{{row.AdvertFlag}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "RealAdvertFlag",
        "type": "boolean",
        "title": "RealAdvertFlag",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search RealAdvertFlag"
        },
        "viewSettings": {
          "mapper": "{{row.RealAdvertFlag}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "EarliestAdvertDate",
        "type": "date",
        "title": "EarliestAdvertDate",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search EarliestAdvertDate"
        },
        "viewSettings": {
          "mapper": "{{row.EarliestAdvertDate}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "EarliestRealAdvertDate",
        "type": "date",
        "title": "EarliestRealAdvertDate",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search EarliestRealAdvertDate"
        },
        "viewSettings": {
          "mapper": "{{row.EarliestRealAdvertDate}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "todo",
        "type": "boolean",
        "title": "todo",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search todo"
        },
        "viewSettings": {
          "mapper": "{{row.todo}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "last_eventID",
        "type": "string",
        "title": "last_eventID",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search last_eventID"
        },
        "viewSettings": {
          "mapper": "{{row.last_eventID}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "PortOfLoading_Lima",
        "type": "string",
        "title": "PortOfLoading_Lima",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search PortOfLoading_Lima"
        },
        "viewSettings": {
          "mapper": "{{row.PortOfLoading_Lima}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ETD_PortOfLoading",
        "type": "datetime",
        "title": "ETD_PortOfLoading",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ETD_PortOfLoading"
        },
        "viewSettings": {
          "mapper": "{{row.ETD_PortOfLoading}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Terminal_Inland_Vorgabe_NTL",
        "type": "string",
        "title": "Terminal_Inland_Vorgabe_NTL",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Terminal_Inland_Vorgabe_NTL"
        },
        "viewSettings": {
          "mapper": "{{row.Terminal_Inland_Vorgabe_NTL}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ETA_Terminal_Inland",
        "type": "datetime",
        "title": "ETA_Terminal_Inland",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ETA_Terminal_Inland"
        },
        "viewSettings": {
          "mapper": "{{row.ETA_Terminal_Inland}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "DeliveryAddress",
        "type": "string",
        "title": "DeliveryAddress",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search DeliveryAddress"
        },
        "viewSettings": {
          "mapper": "{{row.DeliveryAddress}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ETA_DeliveryAddress",
        "type": "datetime",
        "title": "ETA_DeliveryAddress",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ETA_DeliveryAddress"
        },
        "viewSettings": {
          "mapper": "{{row.ETA_DeliveryAddress}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Transport_Mode_Terminal_Vorgabe_NTL",
        "type": "string",
        "title": "Transport_Mode_Terminal_Vorgabe_NTL",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Transport_Mode_Terminal_Vorgabe_NTL"
        },
        "viewSettings": {
          "mapper": "{{row.Transport_Mode_Terminal_Vorgabe_NTL}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Transport_Mode_final_DeliveryAddress",
        "type": "string",
        "title": "Transport_Mode_final_DeliveryAddress",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Transport_Mode_final_DeliveryAddress"
        },
        "viewSettings": {
          "mapper": "{{row.Transport_Mode_final_DeliveryAddress}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ATA_DeliveryAddress",
        "type": "datetime",
        "title": "ATA_DeliveryAddress",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ATA_DeliveryAddress"
        },
        "viewSettings": {
          "mapper": "{{row.ATA_DeliveryAddress}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "transport_Dispo_ID",
        "type": "number",
        "title": "transport_Dispo_ID",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "number",
          "placeholder": "Search transport_Dispo_ID"
        },
        "viewSettings": {
          "mapper": "{{row.transport_Dispo_ID}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "number",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Condition",
        "type": "string",
        "title": "Condition",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Condition"
        },
        "viewSettings": {
          "mapper": "{{row.Condition}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "into_CW1",
        "type": "boolean",
        "title": "into_CW1",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search into_CW1"
        },
        "viewSettings": {
          "mapper": "{{row.into_CW1}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "to_Customer",
        "type": "boolean",
        "title": "to_Customer",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search to_Customer"
        },
        "viewSettings": {
          "mapper": "{{row.to_Customer}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "to_Transporter",
        "type": "boolean",
        "title": "to_Transporter",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search to_Transporter"
        },
        "viewSettings": {
          "mapper": "{{row.to_Transporter}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "DropOff_Terminal",
        "type": "string",
        "title": "DropOff_Terminal",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search DropOff_Terminal"
        },
        "viewSettings": {
          "mapper": "{{row.DropOff_Terminal}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Terminal_PortOfDischarge",
        "type": "string",
        "title": "Terminal_PortOfDischarge",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Terminal_PortOfDischarge"
        },
        "viewSettings": {
          "mapper": "{{row.Terminal_PortOfDischarge}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Pincode",
        "type": "string",
        "title": "Pincode",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Pincode"
        },
        "viewSettings": {
          "mapper": "{{row.Pincode}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "DropOff_Terminal_TIR",
        "type": "string",
        "title": "DropOff_Terminal_TIR",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search DropOff_Terminal_TIR"
        },
        "viewSettings": {
          "mapper": "{{row.DropOff_Terminal_TIR}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Delivered",
        "type": "boolean",
        "title": "Delivered",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search Delivered"
        },
        "viewSettings": {
          "mapper": "{{row.Delivered}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Remarks",
        "type": "string",
        "title": "Remarks",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Remarks"
        },
        "viewSettings": {
          "mapper": "{{row.Remarks}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "HouseBill",
        "type": "string",
        "title": "HouseBill",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search HouseBill"
        },
        "viewSettings": {
          "mapper": "{{row.HouseBill}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Containerverfuegbarkeit",
        "type": "datetime",
        "title": "Containerverfuegbarkeit",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search Containerverfuegbarkeit"
        },
        "viewSettings": {
          "mapper": "{{row.Containerverfuegbarkeit}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ETA_Service_Provider",
        "type": "datetime",
        "title": "ETA_Service_Provider",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ETA_Service_Provider"
        },
        "viewSettings": {
          "mapper": "{{row.ETA_Service_Provider}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ATA_Terminal_Inland",
        "type": "datetime",
        "title": "ATA_Terminal_Inland",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ATA_Terminal_Inland"
        },
        "viewSettings": {
          "mapper": "{{row.ATA_Terminal_Inland}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ATD_Terminal_Inland",
        "type": "datetime",
        "title": "ATD_Terminal_Inland",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ATD_Terminal_Inland"
        },
        "viewSettings": {
          "mapper": "{{row.ATD_Terminal_Inland}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Return_Date_to_Depot",
        "type": "datetime",
        "title": "Return_Date_to_Depot",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search Return_Date_to_Depot"
        },
        "viewSettings": {
          "mapper": "{{row.Return_Date_to_Depot}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ATD_Seaport",
        "type": "datetime",
        "title": "ATD_Seaport",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ATD_Seaport"
        },
        "viewSettings": {
          "mapper": "{{row.ATD_Seaport}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ETD_Seaport",
        "type": "datetime",
        "title": "ETD_Seaport",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ETD_Seaport"
        },
        "viewSettings": {
          "mapper": "{{row.ETD_Seaport}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ETA_Seaport",
        "type": "datetime",
        "title": "ETA_Seaport",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ETA_Seaport"
        },
        "viewSettings": {
          "mapper": "{{row.ETA_Seaport}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ATA_Seaport",
        "type": "datetime",
        "title": "ATA_Seaport",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ATA_Seaport"
        },
        "viewSettings": {
          "mapper": "{{row.ATA_Seaport}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "final_date_delivery_address",
        "type": "datetime",
        "title": "final_date_delivery_address",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search final_date_delivery_address"
        },
        "viewSettings": {
          "mapper": "{{row.final_date_delivery_address}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ctv_booking_id",
        "type": "number",
        "title": "ctv_booking_id",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "number",
          "placeholder": "Search ctv_booking_id"
        },
        "viewSettings": {
          "mapper": "{{row.ctv_booking_id}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "number",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "CTV_ETA_PortOfDischarge",
        "type": "datetime",
        "title": "CTV_ETA_PortOfDischarge",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search CTV_ETA_PortOfDischarge"
        },
        "viewSettings": {
          "mapper": "{{row.CTV_ETA_PortOfDischarge}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "PortOfDischarge_UNLOCO",
        "type": "string",
        "title": "PortOfDischarge_UNLOCO",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search PortOfDischarge_UNLOCO"
        },
        "viewSettings": {
          "mapper": "{{row.PortOfDischarge_UNLOCO}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "PortOfLoading_UNLOCO",
        "type": "string",
        "title": "PortOfLoading_UNLOCO",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search PortOfLoading_UNLOCO"
        },
        "viewSettings": {
          "mapper": "{{row.PortOfLoading_UNLOCO}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ATA_DropOff_Terminal",
        "type": "datetime",
        "title": "ATA_DropOff_Terminal",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ATA_DropOff_Terminal"
        },
        "viewSettings": {
          "mapper": "{{row.ATA_DropOff_Terminal}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "DeliveryAddress_CTV",
        "type": "string",
        "title": "DeliveryAddress_CTV",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search DeliveryAddress_CTV"
        },
        "viewSettings": {
          "mapper": "{{row.DeliveryAddress_CTV}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "PortOfDischarge_UNLOCO_CTV",
        "type": "string",
        "title": "PortOfDischarge_UNLOCO_CTV",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search PortOfDischarge_UNLOCO_CTV"
        },
        "viewSettings": {
          "mapper": "{{row.PortOfDischarge_UNLOCO_CTV}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Transport_Mode_Terminal_CTV",
        "type": "string",
        "title": "Transport_Mode_Terminal_CTV",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Transport_Mode_Terminal_CTV"
        },
        "viewSettings": {
          "mapper": "{{row.Transport_Mode_Terminal_CTV}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Carrier_CTV",
        "type": "string",
        "title": "Carrier_CTV",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Carrier_CTV"
        },
        "viewSettings": {
          "mapper": "{{row.Carrier_CTV}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "customs_document_number",
        "type": "string",
        "title": "customs_document_number",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search customs_document_number"
        },
        "viewSettings": {
          "mapper": "{{row.customs_document_number}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "customs_document_type",
        "type": "string",
        "title": "customs_document_type",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search customs_document_type"
        },
        "viewSettings": {
          "mapper": "{{row.customs_document_type}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Terminal_Inland_CTV",
        "type": "string",
        "title": "Terminal_Inland_CTV",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Terminal_Inland_CTV"
        },
        "viewSettings": {
          "mapper": "{{row.Terminal_Inland_CTV}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ETD_Pickup_PortOfDischarge",
        "type": "datetime",
        "title": "ETD_Pickup_PortOfDischarge",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ETD_Pickup_PortOfDischarge"
        },
        "viewSettings": {
          "mapper": "{{row.ETD_Pickup_PortOfDischarge}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ATD_Pickup_PortOfDischarge",
        "type": "datetime",
        "title": "ATD_Pickup_PortOfDischarge",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ATD_Pickup_PortOfDischarge"
        },
        "viewSettings": {
          "mapper": "{{row.ATD_Pickup_PortOfDischarge}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "pinRequested",
        "type": "date",
        "title": "pinRequested",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search pinRequested"
        },
        "viewSettings": {
          "mapper": "{{row.pinRequested}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "pinState",
        "type": "string",
        "title": "pinState",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search pinState"
        },
        "viewSettings": {
          "mapper": "{{row.pinState}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "nationalTransportState",
        "type": "string",
        "title": "nationalTransportState",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search nationalTransportState"
        },
        "viewSettings": {
          "mapper": "{{row.nationalTransportState}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "finalDeliveryState",
        "type": "string",
        "title": "finalDeliveryState",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search finalDeliveryState"
        },
        "viewSettings": {
          "mapper": "{{row.finalDeliveryState}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "emptyReturnState",
        "type": "string",
        "title": "emptyReturnState",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search emptyReturnState"
        },
        "viewSettings": {
          "mapper": "{{row.emptyReturnState}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ETA_Terminal_Inland_Vorgabe_NTL",
        "type": "datetime",
        "title": "ETA_Terminal_Inland_Vorgabe_NTL",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ETA_Terminal_Inland_Vorgabe_NTL"
        },
        "viewSettings": {
          "mapper": "{{row.ETA_Terminal_Inland_Vorgabe_NTL}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "DeliveryInfo_send_to_CTV",
        "type": "datetime",
        "title": "DeliveryInfo_send_to_CTV",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search DeliveryInfo_send_to_CTV"
        },
        "viewSettings": {
          "mapper": "{{row.DeliveryInfo_send_to_CTV}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "new",
        "type": "boolean",
        "title": "new",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search new"
        },
        "viewSettings": {
          "mapper": "{{row.new}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Transport_Mode_Terminal",
        "type": "string",
        "title": "Transport_Mode_Terminal",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Transport_Mode_Terminal"
        },
        "viewSettings": {
          "mapper": "{{row.Transport_Mode_Terminal}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "CSG_Event_ID",
        "type": "string",
        "title": "CSG_Event_ID",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search CSG_Event_ID"
        },
        "viewSettings": {
          "mapper": "{{row.CSG_Event_ID}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Terminal_Inland_CundA",
        "type": "string",
        "title": "Terminal_Inland_CundA",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Terminal_Inland_CundA"
        },
        "viewSettings": {
          "mapper": "{{row.Terminal_Inland_CundA}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Direct_Truck",
        "type": "boolean",
        "title": "Direct_Truck",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search Direct_Truck"
        },
        "viewSettings": {
          "mapper": "{{row.Direct_Truck}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ETA_DeliveryAddress_CTV",
        "type": "datetime",
        "title": "ETA_DeliveryAddress_CTV",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ETA_DeliveryAddress_CTV"
        },
        "viewSettings": {
          "mapper": "{{row.ETA_DeliveryAddress_CTV}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "EventType",
        "type": "string",
        "title": "EventType",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search EventType"
        },
        "viewSettings": {
          "mapper": "{{row.EventType}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Transport_to_Terminal",
        "type": "string",
        "title": "Transport_to_Terminal",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Transport_to_Terminal"
        },
        "viewSettings": {
          "mapper": "{{row.Transport_to_Terminal}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Seal_Number",
        "type": "string",
        "title": "Seal_Number",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Seal_Number"
        },
        "viewSettings": {
          "mapper": "{{row.Seal_Number}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Transport_Document_Number",
        "type": "string",
        "title": "Transport_Document_Number",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Transport_Document_Number"
        },
        "viewSettings": {
          "mapper": "{{row.Transport_Document_Number}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Relation",
        "type": "string",
        "title": "Relation",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Relation"
        },
        "viewSettings": {
          "mapper": "{{row.Relation}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "User",
        "type": "string",
        "title": "User",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search User"
        },
        "viewSettings": {
          "mapper": "{{row.User}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Cancelation_Send_To_CTV",
        "type": "boolean",
        "title": "Cancelation_Send_To_CTV",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search Cancelation_Send_To_CTV"
        },
        "viewSettings": {
          "mapper": "{{row.Cancelation_Send_To_CTV}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "TAC",
        "type": "boolean",
        "title": "TAC",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search TAC"
        },
        "viewSettings": {
          "mapper": "{{row.TAC}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ETA_Delivery_Address_Update_TimeStamp",
        "type": "datetime",
        "title": "ETA_Delivery_Address_Update_TimeStamp",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ETA_Delivery_Address_Update_TimeStamp"
        },
        "viewSettings": {
          "mapper": "{{row.ETA_Delivery_Address_Update_TimeStamp}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "JobID",
        "type": "number",
        "title": "JobID",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "number",
          "placeholder": "Search JobID"
        },
        "viewSettings": {
          "mapper": "{{row.JobID}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "number",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Tabelle",
        "type": "string",
        "title": "Tabelle",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Tabelle"
        },
        "viewSettings": {
          "mapper": "{{row.Tabelle}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "TAC_sent",
        "type": "datetime",
        "title": "TAC_sent",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search TAC_sent"
        },
        "viewSettings": {
          "mapper": "{{row.TAC_sent}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      }
    ]
  valueType: object
  initialValue: |-
    [
      {
        "prop": "Timestamp",
        "type": "datetime",
        "title": "Timestamp",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search Timestamp"
        },
        "viewSettings": {
          "mapper": "{{row.Timestamp}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Interface",
        "type": "string",
        "title": "Interface",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Interface"
        },
        "viewSettings": {
          "mapper": "{{row.Interface}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "direction",
        "type": "string",
        "title": "direction",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search direction"
        },
        "viewSettings": {
          "mapper": "{{row.direction}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "CustomerID",
        "type": "string",
        "title": "CustomerID",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search CustomerID"
        },
        "viewSettings": {
          "mapper": "{{row.CustomerID}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "MBL_ID",
        "type": "number",
        "title": "MBL_ID",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "number",
          "placeholder": "Search MBL_ID"
        },
        "viewSettings": {
          "mapper": "{{row.MBL_ID}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "number",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ContainerID",
        "type": "number",
        "title": "ContainerID",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "number",
          "placeholder": "Search ContainerID"
        },
        "viewSettings": {
          "mapper": "{{row.ContainerID}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "number",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "MBL_Number",
        "type": "string",
        "title": "MBL_Number",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search MBL_Number"
        },
        "viewSettings": {
          "mapper": "{{row.MBL_Number}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Container_Number",
        "type": "string",
        "title": "Container_Number",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Container_Number"
        },
        "viewSettings": {
          "mapper": "{{row.Container_Number}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Status",
        "type": "string",
        "title": "Status",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Status"
        },
        "viewSettings": {
          "mapper": "{{row.Status}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "TransmissionTimestamp",
        "type": "datetime",
        "title": "TransmissionTimestamp",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search TransmissionTimestamp"
        },
        "viewSettings": {
          "mapper": "{{row.TransmissionTimestamp}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Voyage",
        "type": "string",
        "title": "Voyage",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Voyage"
        },
        "viewSettings": {
          "mapper": "{{row.Voyage}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ContainerSize",
        "type": "string",
        "title": "ContainerSize",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search ContainerSize"
        },
        "viewSettings": {
          "mapper": "{{row.ContainerSize}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Broker",
        "type": "string",
        "title": "Broker",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Broker"
        },
        "viewSettings": {
          "mapper": "{{row.Broker}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Carrier",
        "type": "string",
        "title": "Carrier",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Carrier"
        },
        "viewSettings": {
          "mapper": "{{row.Carrier}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Vessel",
        "type": "string",
        "title": "Vessel",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Vessel"
        },
        "viewSettings": {
          "mapper": "{{row.Vessel}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "PortOfDischarge_Lima",
        "type": "string",
        "title": "PortOfDischarge_Lima",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search PortOfDischarge_Lima"
        },
        "viewSettings": {
          "mapper": "{{row.PortOfDischarge_Lima}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ETA_PortOfDischarge",
        "type": "datetime",
        "title": "ETA_PortOfDischarge",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ETA_PortOfDischarge"
        },
        "viewSettings": {
          "mapper": "{{row.ETA_PortOfDischarge}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "PackingMethod",
        "type": "string",
        "title": "PackingMethod",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search PackingMethod"
        },
        "viewSettings": {
          "mapper": "{{row.PackingMethod}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "TotalWeight",
        "type": "number",
        "title": "TotalWeight",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "number",
          "placeholder": "Search TotalWeight"
        },
        "viewSettings": {
          "mapper": "{{row.TotalWeight}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "number",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "AdvertFlag",
        "type": "boolean",
        "title": "AdvertFlag",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search AdvertFlag"
        },
        "viewSettings": {
          "mapper": "{{row.AdvertFlag}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "RealAdvertFlag",
        "type": "boolean",
        "title": "RealAdvertFlag",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search RealAdvertFlag"
        },
        "viewSettings": {
          "mapper": "{{row.RealAdvertFlag}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "EarliestAdvertDate",
        "type": "date",
        "title": "EarliestAdvertDate",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search EarliestAdvertDate"
        },
        "viewSettings": {
          "mapper": "{{row.EarliestAdvertDate}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "EarliestRealAdvertDate",
        "type": "date",
        "title": "EarliestRealAdvertDate",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search EarliestRealAdvertDate"
        },
        "viewSettings": {
          "mapper": "{{row.EarliestRealAdvertDate}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "todo",
        "type": "boolean",
        "title": "todo",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search todo"
        },
        "viewSettings": {
          "mapper": "{{row.todo}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "last_eventID",
        "type": "string",
        "title": "last_eventID",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search last_eventID"
        },
        "viewSettings": {
          "mapper": "{{row.last_eventID}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "PortOfLoading_Lima",
        "type": "string",
        "title": "PortOfLoading_Lima",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search PortOfLoading_Lima"
        },
        "viewSettings": {
          "mapper": "{{row.PortOfLoading_Lima}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ETD_PortOfLoading",
        "type": "datetime",
        "title": "ETD_PortOfLoading",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ETD_PortOfLoading"
        },
        "viewSettings": {
          "mapper": "{{row.ETD_PortOfLoading}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Terminal_Inland_Vorgabe_NTL",
        "type": "string",
        "title": "Terminal_Inland_Vorgabe_NTL",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Terminal_Inland_Vorgabe_NTL"
        },
        "viewSettings": {
          "mapper": "{{row.Terminal_Inland_Vorgabe_NTL}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ETA_Terminal_Inland",
        "type": "datetime",
        "title": "ETA_Terminal_Inland",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ETA_Terminal_Inland"
        },
        "viewSettings": {
          "mapper": "{{row.ETA_Terminal_Inland}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "DeliveryAddress",
        "type": "string",
        "title": "DeliveryAddress",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search DeliveryAddress"
        },
        "viewSettings": {
          "mapper": "{{row.DeliveryAddress}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ETA_DeliveryAddress",
        "type": "datetime",
        "title": "ETA_DeliveryAddress",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ETA_DeliveryAddress"
        },
        "viewSettings": {
          "mapper": "{{row.ETA_DeliveryAddress}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Transport_Mode_Terminal_Vorgabe_NTL",
        "type": "string",
        "title": "Transport_Mode_Terminal_Vorgabe_NTL",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Transport_Mode_Terminal_Vorgabe_NTL"
        },
        "viewSettings": {
          "mapper": "{{row.Transport_Mode_Terminal_Vorgabe_NTL}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Transport_Mode_final_DeliveryAddress",
        "type": "string",
        "title": "Transport_Mode_final_DeliveryAddress",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Transport_Mode_final_DeliveryAddress"
        },
        "viewSettings": {
          "mapper": "{{row.Transport_Mode_final_DeliveryAddress}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ATA_DeliveryAddress",
        "type": "datetime",
        "title": "ATA_DeliveryAddress",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ATA_DeliveryAddress"
        },
        "viewSettings": {
          "mapper": "{{row.ATA_DeliveryAddress}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "transport_Dispo_ID",
        "type": "number",
        "title": "transport_Dispo_ID",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "number",
          "placeholder": "Search transport_Dispo_ID"
        },
        "viewSettings": {
          "mapper": "{{row.transport_Dispo_ID}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "number",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Condition",
        "type": "string",
        "title": "Condition",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Condition"
        },
        "viewSettings": {
          "mapper": "{{row.Condition}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "into_CW1",
        "type": "boolean",
        "title": "into_CW1",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search into_CW1"
        },
        "viewSettings": {
          "mapper": "{{row.into_CW1}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "to_Customer",
        "type": "boolean",
        "title": "to_Customer",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search to_Customer"
        },
        "viewSettings": {
          "mapper": "{{row.to_Customer}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "to_Transporter",
        "type": "boolean",
        "title": "to_Transporter",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search to_Transporter"
        },
        "viewSettings": {
          "mapper": "{{row.to_Transporter}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "DropOff_Terminal",
        "type": "string",
        "title": "DropOff_Terminal",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search DropOff_Terminal"
        },
        "viewSettings": {
          "mapper": "{{row.DropOff_Terminal}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Terminal_PortOfDischarge",
        "type": "string",
        "title": "Terminal_PortOfDischarge",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Terminal_PortOfDischarge"
        },
        "viewSettings": {
          "mapper": "{{row.Terminal_PortOfDischarge}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Pincode",
        "type": "string",
        "title": "Pincode",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Pincode"
        },
        "viewSettings": {
          "mapper": "{{row.Pincode}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "DropOff_Terminal_TIR",
        "type": "string",
        "title": "DropOff_Terminal_TIR",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search DropOff_Terminal_TIR"
        },
        "viewSettings": {
          "mapper": "{{row.DropOff_Terminal_TIR}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Delivered",
        "type": "boolean",
        "title": "Delivered",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search Delivered"
        },
        "viewSettings": {
          "mapper": "{{row.Delivered}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Remarks",
        "type": "string",
        "title": "Remarks",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Remarks"
        },
        "viewSettings": {
          "mapper": "{{row.Remarks}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "HouseBill",
        "type": "string",
        "title": "HouseBill",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search HouseBill"
        },
        "viewSettings": {
          "mapper": "{{row.HouseBill}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Containerverfuegbarkeit",
        "type": "datetime",
        "title": "Containerverfuegbarkeit",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search Containerverfuegbarkeit"
        },
        "viewSettings": {
          "mapper": "{{row.Containerverfuegbarkeit}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ETA_Service_Provider",
        "type": "datetime",
        "title": "ETA_Service_Provider",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ETA_Service_Provider"
        },
        "viewSettings": {
          "mapper": "{{row.ETA_Service_Provider}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ATA_Terminal_Inland",
        "type": "datetime",
        "title": "ATA_Terminal_Inland",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ATA_Terminal_Inland"
        },
        "viewSettings": {
          "mapper": "{{row.ATA_Terminal_Inland}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ATD_Terminal_Inland",
        "type": "datetime",
        "title": "ATD_Terminal_Inland",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ATD_Terminal_Inland"
        },
        "viewSettings": {
          "mapper": "{{row.ATD_Terminal_Inland}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Return_Date_to_Depot",
        "type": "datetime",
        "title": "Return_Date_to_Depot",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search Return_Date_to_Depot"
        },
        "viewSettings": {
          "mapper": "{{row.Return_Date_to_Depot}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ATD_Seaport",
        "type": "datetime",
        "title": "ATD_Seaport",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ATD_Seaport"
        },
        "viewSettings": {
          "mapper": "{{row.ATD_Seaport}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ETD_Seaport",
        "type": "datetime",
        "title": "ETD_Seaport",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ETD_Seaport"
        },
        "viewSettings": {
          "mapper": "{{row.ETD_Seaport}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ETA_Seaport",
        "type": "datetime",
        "title": "ETA_Seaport",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ETA_Seaport"
        },
        "viewSettings": {
          "mapper": "{{row.ETA_Seaport}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ATA_Seaport",
        "type": "datetime",
        "title": "ATA_Seaport",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ATA_Seaport"
        },
        "viewSettings": {
          "mapper": "{{row.ATA_Seaport}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "final_date_delivery_address",
        "type": "datetime",
        "title": "final_date_delivery_address",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search final_date_delivery_address"
        },
        "viewSettings": {
          "mapper": "{{row.final_date_delivery_address}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ctv_booking_id",
        "type": "number",
        "title": "ctv_booking_id",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "number",
          "placeholder": "Search ctv_booking_id"
        },
        "viewSettings": {
          "mapper": "{{row.ctv_booking_id}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "number",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "CTV_ETA_PortOfDischarge",
        "type": "datetime",
        "title": "CTV_ETA_PortOfDischarge",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search CTV_ETA_PortOfDischarge"
        },
        "viewSettings": {
          "mapper": "{{row.CTV_ETA_PortOfDischarge}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "PortOfDischarge_UNLOCO",
        "type": "string",
        "title": "PortOfDischarge_UNLOCO",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search PortOfDischarge_UNLOCO"
        },
        "viewSettings": {
          "mapper": "{{row.PortOfDischarge_UNLOCO}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "PortOfLoading_UNLOCO",
        "type": "string",
        "title": "PortOfLoading_UNLOCO",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search PortOfLoading_UNLOCO"
        },
        "viewSettings": {
          "mapper": "{{row.PortOfLoading_UNLOCO}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ATA_DropOff_Terminal",
        "type": "datetime",
        "title": "ATA_DropOff_Terminal",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ATA_DropOff_Terminal"
        },
        "viewSettings": {
          "mapper": "{{row.ATA_DropOff_Terminal}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "DeliveryAddress_CTV",
        "type": "string",
        "title": "DeliveryAddress_CTV",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search DeliveryAddress_CTV"
        },
        "viewSettings": {
          "mapper": "{{row.DeliveryAddress_CTV}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "PortOfDischarge_UNLOCO_CTV",
        "type": "string",
        "title": "PortOfDischarge_UNLOCO_CTV",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search PortOfDischarge_UNLOCO_CTV"
        },
        "viewSettings": {
          "mapper": "{{row.PortOfDischarge_UNLOCO_CTV}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Transport_Mode_Terminal_CTV",
        "type": "string",
        "title": "Transport_Mode_Terminal_CTV",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Transport_Mode_Terminal_CTV"
        },
        "viewSettings": {
          "mapper": "{{row.Transport_Mode_Terminal_CTV}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Carrier_CTV",
        "type": "string",
        "title": "Carrier_CTV",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Carrier_CTV"
        },
        "viewSettings": {
          "mapper": "{{row.Carrier_CTV}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "customs_document_number",
        "type": "string",
        "title": "customs_document_number",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search customs_document_number"
        },
        "viewSettings": {
          "mapper": "{{row.customs_document_number}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "customs_document_type",
        "type": "string",
        "title": "customs_document_type",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search customs_document_type"
        },
        "viewSettings": {
          "mapper": "{{row.customs_document_type}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Terminal_Inland_CTV",
        "type": "string",
        "title": "Terminal_Inland_CTV",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Terminal_Inland_CTV"
        },
        "viewSettings": {
          "mapper": "{{row.Terminal_Inland_CTV}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ETD_Pickup_PortOfDischarge",
        "type": "datetime",
        "title": "ETD_Pickup_PortOfDischarge",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ETD_Pickup_PortOfDischarge"
        },
        "viewSettings": {
          "mapper": "{{row.ETD_Pickup_PortOfDischarge}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ATD_Pickup_PortOfDischarge",
        "type": "datetime",
        "title": "ATD_Pickup_PortOfDischarge",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ATD_Pickup_PortOfDischarge"
        },
        "viewSettings": {
          "mapper": "{{row.ATD_Pickup_PortOfDischarge}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "pinRequested",
        "type": "date",
        "title": "pinRequested",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search pinRequested"
        },
        "viewSettings": {
          "mapper": "{{row.pinRequested}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "pinState",
        "type": "string",
        "title": "pinState",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search pinState"
        },
        "viewSettings": {
          "mapper": "{{row.pinState}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "nationalTransportState",
        "type": "string",
        "title": "nationalTransportState",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search nationalTransportState"
        },
        "viewSettings": {
          "mapper": "{{row.nationalTransportState}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "finalDeliveryState",
        "type": "string",
        "title": "finalDeliveryState",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search finalDeliveryState"
        },
        "viewSettings": {
          "mapper": "{{row.finalDeliveryState}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "emptyReturnState",
        "type": "string",
        "title": "emptyReturnState",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search emptyReturnState"
        },
        "viewSettings": {
          "mapper": "{{row.emptyReturnState}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ETA_Terminal_Inland_Vorgabe_NTL",
        "type": "datetime",
        "title": "ETA_Terminal_Inland_Vorgabe_NTL",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ETA_Terminal_Inland_Vorgabe_NTL"
        },
        "viewSettings": {
          "mapper": "{{row.ETA_Terminal_Inland_Vorgabe_NTL}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "DeliveryInfo_send_to_CTV",
        "type": "datetime",
        "title": "DeliveryInfo_send_to_CTV",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search DeliveryInfo_send_to_CTV"
        },
        "viewSettings": {
          "mapper": "{{row.DeliveryInfo_send_to_CTV}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "new",
        "type": "boolean",
        "title": "new",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search new"
        },
        "viewSettings": {
          "mapper": "{{row.new}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Transport_Mode_Terminal",
        "type": "string",
        "title": "Transport_Mode_Terminal",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Transport_Mode_Terminal"
        },
        "viewSettings": {
          "mapper": "{{row.Transport_Mode_Terminal}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "CSG_Event_ID",
        "type": "string",
        "title": "CSG_Event_ID",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search CSG_Event_ID"
        },
        "viewSettings": {
          "mapper": "{{row.CSG_Event_ID}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Terminal_Inland_CundA",
        "type": "string",
        "title": "Terminal_Inland_CundA",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Terminal_Inland_CundA"
        },
        "viewSettings": {
          "mapper": "{{row.Terminal_Inland_CundA}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Direct_Truck",
        "type": "boolean",
        "title": "Direct_Truck",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search Direct_Truck"
        },
        "viewSettings": {
          "mapper": "{{row.Direct_Truck}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ETA_DeliveryAddress_CTV",
        "type": "datetime",
        "title": "ETA_DeliveryAddress_CTV",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ETA_DeliveryAddress_CTV"
        },
        "viewSettings": {
          "mapper": "{{row.ETA_DeliveryAddress_CTV}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "EventType",
        "type": "string",
        "title": "EventType",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search EventType"
        },
        "viewSettings": {
          "mapper": "{{row.EventType}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Transport_to_Terminal",
        "type": "string",
        "title": "Transport_to_Terminal",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Transport_to_Terminal"
        },
        "viewSettings": {
          "mapper": "{{row.Transport_to_Terminal}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Seal_Number",
        "type": "string",
        "title": "Seal_Number",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Seal_Number"
        },
        "viewSettings": {
          "mapper": "{{row.Seal_Number}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Transport_Document_Number",
        "type": "string",
        "title": "Transport_Document_Number",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Transport_Document_Number"
        },
        "viewSettings": {
          "mapper": "{{row.Transport_Document_Number}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Relation",
        "type": "string",
        "title": "Relation",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Relation"
        },
        "viewSettings": {
          "mapper": "{{row.Relation}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "User",
        "type": "string",
        "title": "User",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search User"
        },
        "viewSettings": {
          "mapper": "{{row.User}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Cancelation_Send_To_CTV",
        "type": "boolean",
        "title": "Cancelation_Send_To_CTV",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search Cancelation_Send_To_CTV"
        },
        "viewSettings": {
          "mapper": "{{row.Cancelation_Send_To_CTV}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "TAC",
        "type": "boolean",
        "title": "TAC",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search TAC"
        },
        "viewSettings": {
          "mapper": "{{row.TAC}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "ETA_Delivery_Address_Update_TimeStamp",
        "type": "datetime",
        "title": "ETA_Delivery_Address_Update_TimeStamp",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search ETA_Delivery_Address_Update_TimeStamp"
        },
        "viewSettings": {
          "mapper": "{{row.ETA_Delivery_Address_Update_TimeStamp}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "JobID",
        "type": "number",
        "title": "JobID",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "number",
          "placeholder": "Search JobID"
        },
        "viewSettings": {
          "mapper": "{{row.JobID}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "number",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "Tabelle",
        "type": "string",
        "title": "Tabelle",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "text",
          "placeholder": "Search Tabelle"
        },
        "viewSettings": {
          "mapper": "{{row.Tabelle}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "text",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      },
      {
        "prop": "TAC_sent",
        "type": "datetime",
        "title": "TAC_sent",
        "sortable": true,
        "enableEdit": true,
        "showFilter": true,
        "filter": {
          "type": "date",
          "placeholder": "Search TAC_sent"
        },
        "viewSettings": {
          "mapper": "{{row.TAC_sent}}",
          "color": "{{row.status === 'active' ? 'success' : 'danger'}}"
        },
        "editSettings": {
          "inputType": "date",
          "required": false,
          "validation": {
            "minLength": 0,
            "maxLength": 255,
            "errorMessage": "Invalid value."
          }
        },
        "conditionalFormatting": [
          {
            "condition": "{{row.status === 'delayed'}}",
            "color": "warning"
          },
          {
            "condition": "{{row.status === 'error'}}",
            "color": "danger"
          }
        ],
        "triggers": {
          "onClick": "{{console.log('Row clicked:', row)}}"
        }
      }
    ]
  version: 1
selected: false
configured: true
config:
  preserveStateOnPageChange: true
parentId: '17603437805180.30971559546049277'
